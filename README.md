# bird-classifier
A image detection AI using HuggingFace

How to run:
Install the requirements by running pip install `requirements.txt`  
Once you've installed the dependencies run `main.py` and it will begin training  
If you want to test diffrent images replace `test1.png` with a different image or copy lines `143`-`159` and change the image  
